{
  "name": "{{PROJECTNAME}}",
  "version": "{{VERSION}}",
  "type": "module",
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist",
    "test": "bun test",
    "lint": "biome check .",
    "format": "biome format . --write",
    "format:check": "biome format . --check",
    "audit": "bun audit"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "graphql-yoga": "^5.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "vitest": "^1.0.0",
    "@biomejs/biome": "^1.5.0"
  }
}
